# ComfyUI-PromptPalette-F dependencies.
#
# googletrans is required by the "Prompt Tabs + Translate" node, which uses it
# to translate text (no API key needed). When googletrans is missing or fails,
# the node simply returns an empty translation.

googletrans
